tasks() {
  var target = 3;
  var nums = [1, 2, 3, 4, 5];

  for (var i = 0; i < nums.length; i++) {
    print(i);
    // final result = nums[i] + nums[i++];
    // print("${nums[i]}: $result");
  }
  // final result = nums[0] + nums[5];
}
